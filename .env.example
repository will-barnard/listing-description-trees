DB_PASSWORD=

# Random secret used to sign session JWTs. Generate one with:
#   openssl rand -hex 32
JWT_SECRET=

# Set to true to bypass auth entirely (local dev only — never use in production)
AUTH_DISABLED=false
AUTH_COOKIE_NAME=auth_token
